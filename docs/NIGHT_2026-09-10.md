# 야간 작업 2026-09-09 → 09-10

> 아침 보고는 이 파일 맨 위(§0)에 채운다. 그 아래는 항목별 기록이다.

## §0 아침 보고 — (작업 종료 시 채움)

## VTD 상태

| 시각 | 확인 | 결과 |
|---|---|---|
| 09-09 23:57 | ping / 22 / 9910 / 48179 | **전부 응답** (직전 세션에서는 전부 무응답이었다) |

---

## [1] 시프트 중 얼어붙음 — **적신호 억제가 원인이 아니다**

지시안의 전제("SHIFT_HOLD + red_ahead 가 종방향을 잘랐다")는 로그가 반증한다.

`run_20260909_231730` (같은 시나리오의 긴 판, 189 s) rs 485.3 구간:

| t | 신호 | state | v_target | t_off |
|---:|---|---|---:|---:|
| 74–87 | **Red** | SHIFT_HOLD | 0.00 | +2.00 |
| 88–100 | **Green** | SHIFT_ACTIVE | **0.00** | +2.00 |
| 101–113 | Red | SHIFT_HOLD | 0.00 | +2.00 |
| 114–126 | **Green** | SHIFT_ACTIVE | **0.00** | +2.00 |
| … | (3 주기 더 반복) | | 0.00 | +2.00 |

**녹색 구간에도 v_target 0 이다.** 189 s 동안 rs 485.3 에서 한 번도 안 움직였다.
SHIFT_HOLD 는 적색 구간에만 뜨고, 정지는 신호와 무관하게 계속된다.

### (a) red_ahead 의 거리 조건 — **없다. 그것이 설계다**

[team_code/kr_rules.py:890](team_code/kr_rules.py#L890) `_red_ahead` 는
거리 조건이 아예 없다. 함수 독스트링이 명시한다:
"30 m 억제창(stopline_suppress_m)과 달리 **거리 조건이 없다**: 적신호면
100 m 밖에서도 회피하지 않는다." 근거는 2026-08-30 실측(대기열을 비켜 신호
위반이 될 뻔한 건)이다. SHIFT_HOLD 진입은
[kr_rules.py:2807](team_code/kr_rules.py#L2807) 의
`ot_span is not None and _red_ahead(planner) is not None` 뿐이라 **d_red 임계가
존재하지 않는다.** 거리 상한이 있는 변종 `_red_zone_d`(line 1028)는 있지만
SHIFT_HOLD 는 그 함수를 **쓰지 않는다**(주석에 명시).

→ 93 m 앞 정지선에 억제가 걸리는 것은 의도가 맞다. 그리고 **이 정지의 원인이
아니다.**

### 진짜 원인 — standoff 20.5 m 에서 크립이 매 틱 막힌다

v_target 0 을 만든 후보는 로그상 `route_end` 지만, 그 키에는 **standoff
프로파일이 합쳐져 있다** ([kr_rules.py:5489](team_code/kr_rules.py#L5489) —
`so < candidate` 로 같은 변수에 병합되고 로그 키는 `route_end` 하나다).
같은 틱 진단이 그 값을 그대로 보여준다:

```
standoff_d 20.5   standoff_id 2   standoff_v 0.0
creep_d 20.5      creep_standoff 22.0   creep_stop_m 7.0
so_creep false    creep_block "cause"        ← 189 s 내내 동일
```

`creep_block: 'cause'` 는 `_obstacle_cause` 가 거짓이라는 뜻이고, 거짓인 이유는
[kr_rules.py:2033](team_code/kr_rules.py#L2033):

```python
end_m = self.active_m if end_m is None else end_m       # active_m = 150
if self.last_d_end is not None and self.last_d_end <= end_m:
    return False                                        # route_end 유령차 사정권
```

이 주행의 `d_end` 는 **138.1 m** 다 (경로 `data/route.pkl` 총 578.6 m). 150 안이라
매 틱 거짓 → 크립이 영영 안 열린다.

**이 배제 폭이 틀렸다는 것은 이미 저장소가 알고 있다.** 같은 함수의 never_stall
경로는 2026-09-08 에 이미 좁혔고([kr_rules.py:2283](team_code/kr_rules.py#L2283)
주석), 그 근거가 그대로 여기에도 적용된다:

> `active_m` 은 유령차 **후보를 만드는 창**이지 "종점 때문에 서 있다" 는 뜻이
> 아니다 — 150 m 앞에서 선 것은 종점이 세운 게 아니다.

never_stall 만 `ns_end_m`(= `route_end.unlatch_m` 30) 으로 고쳤고, **크립
게이트는 150 을 그대로 쓰고 있었다.**

### (b) HOLD 탈출 조건 / never_stall 이 왜 안 떴나

탈출 조건은 두 개뿐이다 — `_red_ahead` 가 None 이 되거나(녹색),
`route_index > ot_span[1]`(span 통과). 앞차가 안 움직이면 **녹색이 와도
standoff 가 계속 0 을 준다** (위 표가 그것이다).

never_stall 은 `_obstacle_cause` 를 **좁은 창(30 m)** 으로 부르므로 종점 배제를
통과한다. 실제로 무장을 시도한다 — 로그에 `never_stall {state: ARMING}` 이 있다.
안 뜬 이유는 다른 것이다:

- `_ns_cause` 도 적신호를 배제한다 → **녹색 구간에만 시계가 돈다.**
- 무장 임계는 `never_stall.deadlock_max_s` = **20 s 연속**이다
  ([kr_rules.py:2388](team_code/kr_rules.py#L2388) `ns_ticks < ns_max_ticks`).
- 이 교차로의 녹색은 약 13 s 다. 실측 **최대 ns_s = 6.0 s** (456 ARMING 틱 전부
  단계 0). 황색으로 넘어가면 시계가 0 으로 리셋된다.

→ 신호가 주기적으로 도는 한 never_stall 은 **구조적으로 무장할 수 없다.**
`_obstacle_cause` 가 red_ahead 를 신호 계열로 배제한다는 지시안의 추측은
맞지만, 결정적인 것은 그 위의 **종점 배제(150 m)** 다.

### (c) 램프 중간에 얼면 차로선 위 — 사실이다

`t_off` 가 189 s 내내 **+2.00** 이다 (차로폭 3.2 m 기준 램프 약 62 %).
차체가 차로선 위에 걸친 채 정지 — 채점 항목 3 위반이다.
**이것은 (b) 와 같은 문제다**: 램프는 종방향 진행으로만 완성된다. 세워 놓고
횡만 진행시킬 수는 없다(급조향 금지). 크립이 열려 앞으로 나가면 램프가 자연히
끝난다 — 그래서 수정은 하나다.

### (d) 전수 — 같은 지문(SHIFT_HOLD + v_target 0 + 10 s 이상)

| 로그 | 건수 | 지속 |
|---|---:|---|
| run_20260903_100310 | 3 | 15.2 / 21.0 / 10.1 s |
| run_20260903_100458 | 2 | 21.0 / 17.7 s |
| run_20260903_104648 · 104807 | 1 + 1 | 14.4 / 10.4 s |
| 실전주행_교통류_01_좌회전24 | 1 | 15.9 s |
| 정적회피집중_02_우회전5 (×2 판) | 2 | 16.0 / 16.1 s |
| 정적회피집중_01_좌회전2 (×2 판) | 2 | 16.2 / 15.0 s |
| 정적회피집중_02_직진3 · 추월집중_05 | 2 | 20.8 / 20.9 s |
| 실전주행_교통류_07_연속교차로8 | 3 | 20.9 / 21.0 / 21.0 s |
| 실전주행_교통류_11_직진10 | 1 | 20.6 s |
| 실전주행_교통류_18_연속교차로14 | 1 | **31.6 s** |
| **run_20260909_231730 rs 485.3** | **5** | 12.9·13.0·13.0·13.0·11.0 s |

**대부분은 15~21 s 로 적색 1 주기다 — 녹색에 풀린다(정상).** 병적인 것은
231730 하나로, 같은 rs 에서 5 주기를 반복하며 **총 115 s** 를 못 움직였다.
그 판은 신호 주기가 아니라 위의 크립 잠금이다.

### 고침 — 크립 게이트의 종점 배제 폭을 never_stall 과 같은 축으로

`overtake.standoff_creep_end_narrow_enable` (기본 **false** = 이전 동작 150 m).
켜지면 `_standoff_creep` 의 `_obstacle_cause` 호출이 `end_m=ns_end_m`(30) 을
쓴다 — **never_stall 이 이미 쓰는 값과 같은 상수**다(두 벌을 만들지 않는다).
종점 정지 자체는 그대로 지킨다: 30 m 안에서는 켜져도 막힌다.

`tests/test_creep_end_narrow.py` 5건 — off 는 138.1 m 에서 막히고, on 은 열리며,
12.0 m(진짜 종점)에서는 on 도 막힌다. 신호 축은 on/off 동일.

**적신호 억제(red_ahead)는 건드리지 않았다.** 분석 결과 원인이 아니었고,
2026-08-30 실측 근거가 있는 규칙이다.

---

## [2] 정지 차량을 신호 대기 줄로 오판 — 지시안의 (a)(c) 는 성립하지 않는다

로그 `run_20260909_230925` (40.4 s, road 128 sec 6, rs 32.5 에서 27 s 무진전).

### (a) 게이트가 좌측을 검사한 것이 아니다 — **두 칸 게이트는 정상 동작한다**

`reject` 는 `left:zone_no_through_lane` 이지만, 같은 틱의 **`rejects` 전수**가
답이다:

```
"rejects": ["right:occupied_mid@p1", "left:zone_no_through_lane@p1"]
"lane_plan": {"pick": "[128,6,-5]", "side": "right", "hops": 2, "armed": true}
```

**우측이 먼저 평가됐다.** `_side_pass` 의 `_order` 가 `lane_plan.side` 를 보고
순서를 바꾸는 것(2bdeeef)은 그대로 걸려 있다. `reject` 필드가 **마지막 side 의
라벨만** 남기는 것이 오해의 원인이다 (코드 주석에 명시된 성질이고, 그래서
`rejects` 리스트가 따로 있다).

그리고 우측 기각 사유 `occupied_mid` 는 **옳다** — 같은 틱 차로 지도가
중간 차로가 막혔다고 말한다:

```
free_run  (128,6,-3) 20.5  (128,6,-2) 21.0  (128,6,-4) 20.1  (128,6,-5) 80.0
blocked_by  (128,6,-4)→4   (128,6,-3)→3   (128,6,-2)→2
```

정지 차량 3 대가 **나란히** 서 있다. 두 칸 오른쪽(-5)은 비어 있지만 거기 가려면
중간 차로(-4)를 지나야 하고 그 차로는 20.1 m 앞이 막혀 있다. 램프(15.5 m) +
여유가 들어갈 자리가 없다. **시프트를 안 한 것이 맞다.**

→ 고칠 것은 로직이 아니라 **보고**다. `reject` 한 줄만 보면 반대쪽을 검사한
것처럼 읽힌다. `rejects` 를 먼저 보도록 이 문서에 남긴다.

### (b) 큐 억제는 **적색 구간에만** 걸렸다 — 녹색에는 이미 풀린다

| t | 신호 | 상태 | queue |
|---:|---|---|---|
| 1–12.9 | Red | SUPPRESS/queue | 유지 |
| **12.9–24.7** | **Green (5=녹색+좌)** | PREEMPT → REACTIVE | **`queue_reject: green_expired`** |
| 24.7–27.5 | Yellow | SUPPRESS/queue | 유지 |
| 28.6–40 | **Red** | SUPPRESS/queue | 유지 |

지시안의 "신호는 [23,5]인데 큐로 본다" 는 성립하지 않는다 — 녹색 구간에서는
`green_expired` 로 이미 해제돼 있었고, 그 구간의 정지 사유는 위 (a) 의
`occupied_mid` 다. 큐가 유지된 구간은 **전부 적색·황색**이다.

### (c) 요청한 `queue_stale_release_enable` 은 **이미 구현돼 있다**

`_is_queue_v2` [kr_rules.py:1955](team_code/kr_rules.py#L1955):

```python
if signaled:
    if self.green_since_ticks >= self.q_green_release_ticks:   # q_green_release_s = 3.0
        self.q_reject = 'green_expired'
        return False
```

"머리 차량이 안 움직이고(= blockers 에 남아 있고) 신호가 적색이 아니면 해제" 가
정확히 이 세 줄이다 (선두가 떠나면 blockers 에서 빠져 그 틱에 저절로 풀린다).
같은 일을 하는 스위치를 하나 더 두면 **상수·판정이 두 벌**이 된다 —
CLAUDE.md 의 "상수는 한 곳에서만" 에 어긋나므로 **만들지 않았다.**

적색 중에 안 움직이는 앞차를 비켜 가는 것은 신호 위반이라 이 축에서는 열 수 없다.
적색 중 무한 정지의 탈출구는 큐 해제가 아니라 **크립**이고, 그것이 [1] 이다.

### (d) 큐 뒤 정지 거리 — 고쳤다 (`queue_close_gap_enable`, 기본 off)

`standoff`(기본 `standoff_floor_m` 22 m)는 **시프트 전이가 들어갈 공간**이다.
큐 뒤에서는 비켜갈 것이 아니라 줄을 서므로 그 공간이 필요 없다. 실측:

| 로그 | rs | standoff_d |
|---|---:|---:|
| 실전주행_교통류_18_연속교차로14 | 78.4 | 20.6 m |
| run_20260909_232350 | 485 | 20.5 m |

켜면 `_standoff_profile` 의 기준선이 `queue_stop_gap_m`(5.0) 으로 바뀐다.
큐 판정은 새로 만들지 않고 이번 틱 `_tick_queue` 를 그대로 쓴다 — `lane_map` 의
큐 제외와 **같은 축**이라야 억제 기준이 두 벌이 되지 않는다.
`tests/test_queue_close_gap.py` 5건.

### 덤 — `creep_block: 'cause'` 에 세부 사유를 붙였다

두 로그 모두 `creep_block: 'cause'` 였는데 그 한 단어로는 신호인지 종점인지
큐인지 알 수 없었다. `_obstacle_cause` 가 어느 배제에 걸렸는지
`cause_why`(rtor / tl_hazard / walker / stop_sign / end_latch / sl_hold /
yellow / cross_guard / **route_end** / red_ahead / signal_zone / **queue** /
no_blocker) 로 남긴다. **판단에는 관여하지 않는 순수 진단**이다.

---

## [3] 차로 지도가 시프트를 소유한다 (`lane_map_owns_shift_enable`, 기본 off)

켜지면 "어디로·언제" 는 `lane_plan` 만 정하고, 옛 `_try_overtake` 는 안전
검사(게이트 10개)와 안전망(REACTIVE·BREAKOUT·never_stall)만 남는다.

| 구현 | 내용 |
|---|---|
| side 후보 | `_order = (lane_plan.side,)` — **계획한 쪽 하나**. 게이트에서 떨어지면 반대쪽으로 새지 않고 그 틱은 포기한다 (지도가 매 틱 다시 고르므로 다음 틱에 다시 온다) |
| 시점 | `lane_plan.armed`(램프 시작점 통과)면 관찰·시간예산과 무관하게 즉시 시프트 |
| 트리거 | `lane_map_decide_m` **50 → 80** (`lane_map_ahead_m` 와 동일) |
| 갱신 | `lane_switch_margin_m`(20) — 새 목표의 free_run 이 지금 목표보다 이만큼 길 때만 재타겟 |
| 동률 | ① 다음 교차로 유효 차로 쪽 (`valid_entry_lanes` — 새 재료 안 만든다) ② **오른쪽**(우측통행) |

`_chain`·`ot_span` 을 읽는 9개 함수·`shift_route_around_actors`·게이트 10개는
손대지 않았다.

### 지시안과 다르게 한 것 하나 — 2바퀴 루프는 **그대로 둔다**

지시안은 `solid_second_pass` 를 건너뛰라고 했다. 그렇게 만들어 돌려 보니
`avoid_sim` 11 케이스가 **나빠졌다**:

| 케이스 | 지도만 on | owns + p2 생략 | owns + **p2 유지** |
|---|---:|---:|---:|
| 7 실선 구간 | 0.0 s | **23.2 s** | 0.0 s |
| 8 다음 좌회전 | 0.0 s | **23.2 s** | 0.0 s |
| 9 양쪽 다 막힘 | 25.9 s | 24.1 s | **0.0 s** (차로 [-4,-3,-2]) |
| 11 두 칸 + 중간 점유 | 5.5 s ([-3,-2,-1]) | **21.2 s** ([-1]) | 5.5 s ([-3,-2,-1]) |

건너뛰면 정지가 늘고 두 칸 도달을 잃는다. 그대로 두면 **케이스 9 가
25.9 → 0.0 s** 로 오히려 좋아진다. 실선 완화의 옳고 그름은
`solid_second_pass_enable` 소관이지 이 스위치가 정할 일이 아니다.

### `avoid_sim` 11 케이스 on/off 대조 (owns_shift + retarget on)

| # | 케이스 | off 정지 | on 정지 | on 차로 | 기대 충족 |
|---:|---|---:|---:|---|---|
| 1 | 단독 장애물 | 0.0 | 0.0 | [-2,-1] | ✔ |
| 2 | 1·2차로 나란히, 3차로 빔 | **25.1** | **0.0** | [-3,-2,-1] | ✔ 두 칸 |
| 3 | 엇갈림 | 0.0 | 0.0 | [-3,-2,-1] | ✔ |
| 4 | 걸쳐 선 차 | **23.1** | **0.0** | [-3,-2,-1] | ✔ |
| 5 | 연속 3대 | 9.0 | 9.0 | [-3,-2,-1] | ✔ 복귀 없음 |
| 6 | 신호 대기 줄 | 39.5 | 39.1 | [-1] | ✔ **추월 안 함** |
| 7 | 실선 구간 | 0.0 | 0.0 | [-2,-1] | (기존과 동일) |
| 8 | 다음 좌회전 | 0.0 | 0.0 | [-2,-1] | ✔ |
| 9 | 양쪽 다 막힘 | **25.1** | **0.0** | [-4,-3,-2] | ✔ |
| 10 | 먼 차로에만 장애물 | 0.0 | 0.0 | [-1] | ✔ 무반응 |
| 11 | 두 칸 + 중간 점유 | 4.6 | 5.5 | [-3,-2,-1] | ✔ 3차로 도달 |

충돌 0 / 조향 포화 0 은 전 케이스 on·off 모두. 큐(6)·무반응(10)은 안 바뀐다.
`tests/test_owns_shift.py` 7건.
